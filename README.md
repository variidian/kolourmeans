# kolourmeans
an application that turns locations selected on a satellite map into colour palettes.
# notes
- the naming of this project comes from the words 'colour' as in 'colour palettes' and 'k-means clustering', the algorithm i used to turn the colours from the image into a nice-looking palette.
